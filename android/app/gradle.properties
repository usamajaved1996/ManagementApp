# Enable incremental builds for faster compilation
android.enableBuildConfigAsBytecode=true
android.defaults.buildfeatures.buildconfig=true
android.enableR8.fullMode=false

# Disable PNG crunching (unnecessary for development builds)
android.disableResourceCrunching=true

# Skip duplicated files check
android.disableCheckDuplicateClasses=true 